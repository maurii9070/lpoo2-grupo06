<UserControl x:Class="Vistas.Inscripciones.GestionDeInscripcionesUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="700"> 
    <Grid Background="#F4F4F4" >

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <DockPanel Grid.Row="0" Margin="0,0,0,15">

            <TextBlock Text="Gestión de Inscripciones"
                       FontSize="26"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Name="btnAnularInscripcion"
                        Content="Anular Inscripción"
                        Width="160"
                        Height="36"
                        Margin="0,0,10,0"
                        FontSize="14"
                        Background="#B03A2E"
                        Foreground="White"
                        BorderBrush="#922B21"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="btnAnularInscripcion_Click"/>

                <Button Name="btnAltaInscripcion"
                        Content="Alta Inscripción"
                        Width="150"
                        Height="36"
                        FontSize="14"
                        Background="#2874A6"
                        Foreground="White"
                        BorderBrush="#1F618D"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="btnInscripcion_Click"/>
            </StackPanel>
        </DockPanel>

        
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="6"
                Padding="10"
                BorderBrush="#D0D0D0"
                BorderThickness="1">

            <Grid>

               
                <TextBlock x:Name="txtSinDatos"
                           Text="No existen inscripciones cargadas"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="Gray"
                           Visibility="Collapsed"/>

            
                <DataGrid Name="dgInscripciones"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FAFAFA"
                          AlternationCount="2"
                          BorderThickness="0"
                          VerticalScrollBarVisibility="Auto"
                          RowHeight="30"
                          ColumnHeaderHeight="32"
                          Background="White">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#EAEAEA"/>
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="#C8C8C8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.RowStyle>

            
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FECHA, StringFormat=dd/MM/yyyy}" Width="80"/>
                        <DataGridTextColumn Header="ID Curso" Binding="{Binding ID_Curso}" Width="80"/>
                        <DataGridTextColumn Header="Curso" Binding="{Binding Curso}" Width="160"/>
                        <DataGridTextColumn Header="ID Alumno" Binding="{Binding ID_Alumno}" Width="80"/>
                        <DataGridTextColumn Header="Alumno" Binding="{Binding Alumno}" Width="160"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto"/>
                    </DataGrid.Columns>

                </DataGrid>

            </Grid>
        </Border>

    </Grid>
</UserControl>
