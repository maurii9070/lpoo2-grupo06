<UserControl x:Class="Vistas.Login.LoginUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="336" d:DesignWidth="351">
    <UserControl.Resources>
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#444" />
            <Setter Property="Margin" Value="0 0 0 3"/>
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Height" Value="45"></Setter>
            <Setter Property="FontSize" Value="16"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="Cursor" Value="Hand"></Setter>
            <Setter Property="BorderBrush" Value="Transparent"></Setter>  
        </Style>
    </UserControl.Resources>
    <StackPanel VerticalAlignment="Center" Height="328">
        <TextBlock Text="Inicia Sesión"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#003366"
                           Margin="0 0 0 20"
                           HorizontalAlignment="Center"/>
        <Label Content="Usuario" Name="lblUsuario"/>
        <TextBox x:Name="txtUsuario"
                         Height="36"
                         BorderBrush="#CCC"
                         BorderThickness="1"
                         Padding="5"
                         Margin="0 0 0 10"
                         FontSize="14"/>
        <Label Content="Contraseña"  Name="lblContrasenia" />
        <PasswordBox x:Name="txtPassword"
                             Height="36"
                             BorderBrush="#CCC"
                             BorderThickness="1"
                             Padding="5"
                             Margin="0 0 0 20"
                             FontSize="14"/>
        <Button x:Name="btnIngresar"
                        Content="Ingresar"
                        Background="#FF003366"
                        Foreground="White"
                        Click="btnIngresar_Click" 
                        IsDefault="True"
                       />
        <Button Content="Salir"
                        Name="btnSalir"
                        Margin="0,10,0,0"
                        Background="#FFE6E4E4"
                        Foreground="#003366"
                        Click="btnCancelar_Click" 
                        IsCancel="True"
                        />

    </StackPanel>
</UserControl>
