<UserControl x:Class="Vistas.Alumnos.GestionAlumnoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="GESTIÓN DE ALUMNOS" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="#FF1565C0"/>
        </StackPanel>

        <!-- Botones de Acción -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="btnAltaAlumno" 
                        Content="ALTA DE ALUMNO" 
                        Click="btnAltaAlumno_Click"
                        Background="#FF2196F3" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        Height="40" 
                        Width="200" 
                        Margin="0,10"
                        BorderBrush="#FF1976D2"
                        BorderThickness="1"/>

        </StackPanel>

        <!-- Panel de contenido dinámico -->
        <ContentControl x:Name="contentControl" Grid.Row="2" Margin="0,15,0,0"/>
    </Grid>
</UserControl>
