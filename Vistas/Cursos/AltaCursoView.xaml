<Window x:Class="Vistas.Cursos.AltaCursoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alta Nuevo Curso" 
        Height="420" Width="500"
        WindowStartupLocation="CenterScreen" 
        Background="White"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Vistas;component/Resources/CursosStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Campos del modelo Curso -->

        <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>
        <TextBox   x:Name="txtNombre" Grid.Row="0" Grid.Column="1" />

        <TextBlock Text="Descripción:" Grid.Row="1" Grid.Column="0" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Top"/>
        <TextBox   x:Name="txtDesc"    Grid.Row="1" Grid.Column="1" Style="{StaticResource FormTextAreaStyle}"/>

        <TextBlock Text="Cupo:" Grid.Row="2" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>
        <TextBox   x:Name="txtCupo" Grid.Row="2" Grid.Column="1" PreviewTextInput="txtCupo_PreviewTextInput"/>

        <TextBlock Text="Fecha Inicio:" Grid.Row="3" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>
        <DatePicker x:Name="dpFechaIni" Grid.Row="3" Grid.Column="1"
                    SelectedDateChanged="dpFechaIni_SelectedDateChanged"/>

        <TextBlock Text="Fecha Fin:" Grid.Row="4" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>
        <DatePicker x:Name="dpFechaFin" Grid.Row="4" Grid.Column="1"/>

        <TextBlock Text="Docente:" Grid.Row="5" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>
        <ComboBox  x:Name="cboDocente" Grid.Row="5" Grid.Column="1"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0">

            <Button x:Name="btnGuardar"  Content="Guardar" IsDefault="True"
                    Click="btnGuardar_Click" 
                    Style="{StaticResource SaveButtonStyle}"/>

            <Button x:Name="btnCancelar" Content="Cancelar" IsCancel="True"
                    Margin="10,0,0,0" 
                    Style="{StaticResource CancelButtonStyle}"/>

        </StackPanel>
    </Grid>
</Window>