<Window x:Class="Vistas.Cursos.AltaCursoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alta Curso" Height="326" Width="433"
        WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Campos del modelo Curso -->

        <TextBlock Text="Nombre:"     Grid.Row="0" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox   x:Name="txtNombre" Grid.Row="0" Grid.Column="1" Margin="5 5"/>

        <TextBlock Text="Descripción:" Grid.Row="1" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox   x:Name="txtDesc"    Grid.Row="1" Grid.Column="1" Margin="5 5"
                   AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="60"/>

        <TextBlock Text="Cupo:"       Grid.Row="2" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox   x:Name="txtCupo"   Grid.Row="2" Grid.Column="1" Margin="5 5" />

        <TextBlock Text="Fecha Inicio:" Grid.Row="3" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <DatePicker x:Name="dpFechaIni" Grid.Row="3" Grid.Column="1" Margin="5 5"/>

        <TextBlock Text="Fecha Fin:"  Grid.Row="4" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <DatePicker x:Name="dpFechaFin" Grid.Row="4" Grid.Column="1" Margin="5 5"/>

        <TextBlock Text="Estado:" Grid.Row="5" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <ComboBox  x:Name="cboEstado" Grid.Row="5" Grid.Column="1" Margin="5 5"/>

        <TextBlock Text="Docente:" Grid.Row="6" Grid.Column="0" Margin="0 5" VerticalAlignment="Center"/>
        <ComboBox  x:Name="cboDocente" Grid.Row="6" Grid.Column="1" Margin="5 5"/>

        <!-- Botones -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0 15 0 0">
            <Button x:Name="btnGuardar"  Content="Guardar" Width="90" IsDefault="True"
                    Click="btnGuardar_Click"/>
            <Button x:Name="btnCancelar" Content="Cancelar" Width="90" Margin="10 0 0 0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
