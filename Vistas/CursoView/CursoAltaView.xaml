<UserControl x:Class="Vistas.CursoView.CursoAltaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="465" d:DesignWidth="340">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="ALTA DE CURSO" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#FF1565C0" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15"/>

        <!-- Formulario -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nombre -->
            <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombre" Grid.Row="0" Grid.Column="1" Margin="0,5,0,10" Height="25"/>

            <!-- Descripción -->
            <TextBlock Text="Descripción:" Grid.Row="1" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <TextBox x:Name="txtDescripcion" Grid.Row="1" Grid.Column="1" Margin="0,5,0,10" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

            <!-- Cupo -->
            <TextBlock Text="Cupo:" Grid.Row="2" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCupo" Grid.Row="2" Grid.Column="1" Margin="0,5,0,10" Height="25"/>

            <!-- Fecha Inicio -->
            <TextBlock Text="Fecha Inicio:" Grid.Row="3" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpFechaInicio" Grid.Row="3" Grid.Column="1" Margin="0,5,0,10" Height="25"/>

            <!-- Fecha Fin -->
            <TextBlock Text="Fecha Fin:" Grid.Row="4" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpFechaFin" Grid.Row="4" Grid.Column="1" Margin="0,5,0,10" Height="25"/>

            <!-- Estado -->
            <TextBlock Text="Estado:" Grid.Row="5" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbEstado" Grid.Row="5" Grid.Column="1" Margin="0,5,0,10" Height="25" DisplayMemberPath="Est_Nombre"/>

            <!-- Docente -->
            <TextBlock Text="Docente:" Grid.Row="6" Grid.Column="0" Margin="0,5,10,5" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbDocente" Grid.Row="6" Grid.Column="1" Margin="0,5,0,10" Height="25" DisplayMemberPath="Doc_Apellido"/>
        </Grid>

        <!-- Botón -->
        <Button x:Name="btnGuardar" 
                Content="GUARDAR CURSO" 
                Click="btnGuardar_Click" 
                Grid.Row="2" 
                Background="#FF2196F3" 
                Foreground="White" 
                FontWeight="Bold" 
                Height="35" 
                Width="120" 
                HorizontalAlignment="Center" 
                Margin="0,15,0,0"/>
    </Grid>
</UserControl>